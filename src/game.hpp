#if !defined(SCREEN_WIDTH) && !defined(SCREEN_HEIGHT)

#define SCREEN_WIDTH 240 * 4
#define SCREEN_HEIGHT 160 * 4

#endif

#if !defined(TILE_SIZE) && !defined(BLOCK_SIZE)

#define TILE_SIZE 8 * 4
#define BLOCK_SIZE 16 * 4

#endif

#ifndef FRAME_HATE

#define FRAME_HATE 60

#endif